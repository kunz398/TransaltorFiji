<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="actionBarStyle">@style/FijianAppBarStyle</item> <!--<item name="actionBarStyle">@style/MiwokAppBarStyle</item>-->
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- App bar style -->
    <style name="FijianAppBarStyle" parent="style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <!-- Remove the shadow below the app bar -->
        <item name="elevation">0dp</item>
    </style>

    <!-- Style for a tab that displays a category name -->
    <style name="CategoryTab" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@android:color/white</item>
        <item name="tabSelectedTextColor">@android:color/white</item>
        <item name="tabTextAppearance">@style/CategoryTabTextAppearance</item>
    </style>

    <!-- Text appearance style for a category tab -->
    <style name="CategoryTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textColor">#A8A19E</item>
    </style>

    <!--USED BY ALL BUTTONS-->
    <style name="UsedByAllButtons" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item> <!--HEIGHT-->
        <item name="android:layout_weight">1</item> <!--WIGHT-->
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/TextColorNumber</item>
        <item name="android:textAlignment">viewStart</item>
        <item name="android:padding">10dp</item>


    </style>
    <!-- ******USED BY ALL BUTTONS*********-->

    <!--BUTTON NUMBER TEXT-->
    <style name="NumberBtn" parent="UsedByAllButtons">
        <!-- Customize your theme here. -->
        <item name="android:background">@color/BtnColorNumber </item>
    </style>
    <!--BUTTON Famly TEXT-->
    <style name="familyBtn"  parent="UsedByAllButtons">
        <!-- Customize your theme here. -->
        <item name="android:background">@color/BtnColorFamily </item>
    </style>
    <!--BUTTON Color TEXT-->
    <style name="colorsBtn"  parent="UsedByAllButtons">
        <!-- Customize your theme here. -->
        <item name="android:background">@color/BtnColor_Colors </item>
    </style>
    <!--BUTTON PHRASES TEXT-->
    <style name="phrasesrBtn" parent="UsedByAllButtons">
        <!-- Customize your theme here. -->
        <item name="android:background">@color/BtnColorPhrases </item>
    </style>

</resources>
